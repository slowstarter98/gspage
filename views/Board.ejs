<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />

    <link rel="stylesheet" type="text/css" href="/public/main.css" />
    <link rel="stylesheet" type="text/css" href="/public/board.css" />
    <title>Lab402</title>
    <script
      src="https://kit.fontawesome.com/c3d18ec51c.js"
      crossorigin="anonymous"
    ></script>
    <script src="../public/header.js" defer></script>
  </head>
  <body>
    <%if(사용자 === undefined){ %> <%- include('header.html') %> <%}%>
    <%if(사용자!==undefined){%> <%- include('header-login.html') %> <%}%>

    <div id="body">
      <div id="wrap">
        <section id="board-section">
          <h1>Board</h1>
          <article id="Writing">
            <table>
              <thead>
                <tr>
                  <th>number</th>
                  <th>Title</th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
                <%if(게시물!=null){ for(var i=0 ;i < 5; i++ ){ %>
                  <% if((총게시물갯수-i-페이지*5)>=0){ %>
                <tr>
                  <td><%= i+페이지*5+1 %></td>
                  <td><a href="/posts/<%=총게시물갯수-i-페이지*5+1%>"><%= 게시물[총게시물갯수-i-페이지*5].Title %></a></td>
                  <td></td>
                </tr>
                <%} } }%>
              </tbody>
            </table>
            <hr />
            <div id="page-div ">
              <ul class="page_nums">

                <% if(게시물!=null){ %>

                  <% if(페이지+1 <= 3){ %>
                    <li>
                      <a href="#"> <i class="fa-solid fa-angle-left"></i> </a>
                    </li>
                    <% for(var i=0 ;i < 5; i++ ){ %>
                      <% if(i==페이지){%>
                       <li>
                        <a class="nowPage" href="<%= i+1 %>"> <%= i+1 %> </a>
                       </li>
                      <%}%>
                      <%if(i!=페이지){%>
                       <li>
                        <a  href="<%= i+1 %>"> <%= i+1 %> </a>
                       </li>
                      <% }%>
                    <% }%>
                  <% }%>

                  <% if(페이지+1 > 3) { %>
                    <li>
                      <a href="<%= 페이지-2 %>"> <i class="fa-solid fa-angle-left"></i> </a>
                    </li>
                    <li>
                      <a href="<%= 페이지-1 %>"> <%= 페이지-1 %> </a>
                    </li>
                    <li>
                      <a href="<%= 페이지 %>"> <%= 페이지 %> </a>
                    </li>
                    <li>
                      <a class="nowPage" href="<%= 페이지+1 %>"> <%= 페이지+1 %> </a>
                    </li>
                    <li>
                      <a href="<%= 페이지+2 %>"> <%= 페이지+2 %> </a>
                    </li>
                    <li>
                      <a href="<%= 페이지+3 %>"> <%= 페이지+3 %> </a>
                    </li>

                  <%}%>
                <%}%>

                <li>
                  <a href="<%= 페이지+4 %>"> <i class="fa-solid fa-angle-right"></i> </a>
                </li>

              </ul>
            </div>
           </br></br>
            <span>
            부적절한 내용, 도배 등 사이트 운영 기준 위반의 경우 동의 없이 삭제될
            수 있습니다.
             </span>
            <br />
          </article>

          <div id="lowContents">
            <h3>
              <a class="writing" href="/board-write"> 글 작성하기 </a>
            </h3>
          </div>
        </section>
      </div>
    </div>

    <%- include('footer.html') %>
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.min.js"></script>
  </body>
</html>
